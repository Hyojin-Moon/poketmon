import styled from 'styled-components'

const DashboardContainer = styled.div`
  display: flex;
`

const Dashboard = () => {
  return (
    <DashboardContainer>
      대쉬보드
    </DashboardContainer>
  );
};

export default Dashboard;